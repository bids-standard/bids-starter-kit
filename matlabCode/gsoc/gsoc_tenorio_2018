%% Template Matlab script to create an BIDS compatible _events.tsv file
% This example lists all required and some optional fields.
%
% MTenorio, 2018

%%
clear all
root_dir = 'C:\Users\Marcus\Documents'; %Set manually your root directory
project_label = 'templates';


events_tsv_name = fullfile(root_dir,project_label,...
    '_events.tsv'); %make sure you create a new blank file named '_events.tsv' on tamplates directory

%% make a participants table and save 

onset = [0]'; % onsets in seconds
duration = [0]'; % duration in seconds (measured from onset)
trial_type = {'go'}; % Primary categorisation of each trial 
response_time = [0]'; %Response time measured in seconds.


t = table(onset,duration,trial_type,response_time);

writetable(t,events_tsv_name,'FileType','text','Delimiter','\t');
